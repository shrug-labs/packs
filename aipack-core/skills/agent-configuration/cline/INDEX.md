# Cline harness

## Where rules/workflows live (common patterns)
- `.clinerules/` (rules + workflows)

## MCP configuration
- Cline MCP server configuration: https://docs.cline.bot/mcp/configuring-mcp-servers

## Notes
- Allowlist mechanisms (e.g., `autoApprove`) are client-specific; treat them as policy surfaces.
